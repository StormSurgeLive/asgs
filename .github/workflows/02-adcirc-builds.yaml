name: Build ASGS and All ADCIRCs

on:
  push:
    branches:
      - master
      - issue-1677
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  build-all-adcircs:
    name: ${{ matrix.name }}
    runs-on: ubuntu-22.04

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: ubuntu-22.04-gfortran
            distro: ubuntu
            compiler: gfortran
          - name: rocky-9-gfortran
            distro: rocky
            compiler: gfortran
            container: rockylinux:9

    container: ${{ matrix.container || '' }}

    defaults:
      run:
        shell: bash

    env:
      TERM: xterm
      CI: "true"
      FORCE_CLEAN: "1"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install system packages (Ubuntu)
        if: matrix.distro == 'ubuntu'
        run: |
          set -euxo pipefail
          sudo apt-get update
          sudo apt-get install -y \
            bash \
            bc \
            build-essential \
            bzip2 \
            ca-certificates \
            checkinstall \
            curl \
            file \
            findutils \
            flex \
            gawk \
            git \
            gfortran \
            gzip \
            htop \
            libexpat1-dev \
            libssl-dev \
            make \
            patch \
            procps \
            python3 \
            rsync \
            screen \
            sed \
            tar \
            tmux \
            unzip \
            vim \
            wget \
            zip \
            zlib1g-dev

      - name: Install system packages (Rocky)
        if: matrix.distro == 'rocky'
        run: |
          set -euxo pipefail
          dnf -y update
          dnf -y install \
            bash \
            bc \
            bzip2 \
            ca-certificates \
            curl-minimal \
            diffutils \
            file \
            findutils \
            flex \
            gcc \
            gcc-c++ \
            gcc-gfortran \
            gawk \
            git \
            gzip \
            make \
            openssl-devel \
            patch \
            perl \
            procps-ng \
            python3 \
            redhat-rpm-config \
            rsync \
            sed \
            tar \
            unzip \
            vim-minimal \
            wget \
            which \
            zip \
            zlib-devel

      - name: Prepare writable HOME/bin
        run: |
          set -euxo pipefail
          mkdir -p "$HOME/bin"
          mkdir -p "$GITHUB_WORKSPACE/tmp"

      - name: Flush ASGS install tree
        if: env.FORCE_CLEAN == '1'
        run: |
          set -euxo pipefail
          rm -rf ./opt ./tmp ./profiles
          rm -f ./update-asgs
          rm -f "$HOME/bin/asgsh" "$HOME/bin/update-asgs"

      - name: Bootstrap ASGS dependencies with init-asgs.sh
        run: |
          set -euxo pipefail
          chmod +x ./init-asgs.sh
          ./init-asgs.sh -b -A -c "${{ matrix.compiler }}"

      - name: Verify bootstrap outputs exist
        run: |
          set -euxo pipefail
          test -x ./update-asgs || { echo "update-asgs wrapper was not created"; exit 1; }
          test -d ./opt || { echo "./opt was not created"; exit 1; }
          test -d ./profiles || { echo "./profiles was not created"; exit 1; }
          test -x ./asgsh || { echo "./asgsh was not found or not executable"; exit 1; }

      - name: Locate build-all-adcircs script
        run: |
          set -euxo pipefail
          if [ -x "./cloud/general/t/build-all-adcircs.sh" ]; then
            echo "BUILD_ALL_SCRIPT=./cloud/general/t/build-all-adcircs.sh" >> "$GITHUB_ENV"
          elif [ -f "./cloud/general/t/build-all-adcircs.sh" ]; then
            chmod +x ./cloud/general/t/build-all-adcircs.sh
            echo "BUILD_ALL_SCRIPT=./cloud/general/t/build-all-adcircs.sh" >> "$GITHUB_ENV"
          elif [ -x "./t/build-all-adcircs.sh" ]; then
            echo "BUILD_ALL_SCRIPT=./t/build-all-adcircs.sh" >> "$GITHUB_ENV"
          elif [ -f "./t/build-all-adcircs.sh" ]; then
            chmod +x ./t/build-all-adcircs.sh
            echo "BUILD_ALL_SCRIPT=./t/build-all-adcircs.sh" >> "$GITHUB_ENV"
          else
            echo "Could not find build-all-adcircs.sh"
            exit 1
          fi

      - name: Build all ADCIRCs inside asgsh
        run: |
          set -euxo pipefail
          chmod +x ./asgsh
          ./asgsh -A "
            set -euxo pipefail
            bash \"$BUILD_ALL_SCRIPT\"
          "

      - name: Verify ADCIRCs inside asgsh
        run: |
          set -euxo pipefail
          ./asgsh -A "
            set -euxo pipefail
            list adcirc
            verify adcirc
          "

      - name: Verify ADCIRC binaries were built
        run: |
          set -euxo pipefail
          mapfile -t bins < <(find "$GITHUB_WORKSPACE" -type f \
            \( -name adcirc -o -name padcirc -o -name adcswan -o -name padcswan \) \
            | sort)

          printf 'Discovered binaries:\n'
          if [ "${#bins[@]}" -eq 0 ]; then
            echo "  <none>"
            echo "No ADCIRC binaries were found."
            exit 1
          fi
          printf '  %s\n' "${bins[@]}"

      - name: Upload logs and build artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}-logs-and-builds
          if-no-files-found: warn
          path: |
            update-asgs
            opt/
            profiles/
            tmp/
            **/*.log
            **/cmake*.log
            **/make.log
            **/build.log
            **/adcirc
            **/padcirc
            **/adcswan
            **/padcswan
